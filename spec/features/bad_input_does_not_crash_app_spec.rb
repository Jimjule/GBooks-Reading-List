# frozen_string_literal: true

require 'glist'

describe BookList do
  # Currently replaced by manual test

  # it "Saving doesn't crash with bad input" do
  #   booklist = BookList.new
  #   allow(booklist).to receive(:gets).and_return('Search', '9', '8', '0', '')
    # expect { booklist.go }.not_to raise_error
  # end
end
